name: Trading Engine - LIVE

on:
  workflow_dispatch:

jobs:
  trading:
    runs-on: ubuntu-latest
    steps:
      - name: Trading engine disabled
        run: echo "Trading Engine is no longer part of the production system."
