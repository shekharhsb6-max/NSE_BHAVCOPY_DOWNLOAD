name: Bucket Engine - LIVE

on:
  workflow_dispatch:

jobs:
  bucket-live:
    runs-on: ubuntu-latest
    steps:
      - name: Bucket engine disabled
        run: echo "Bucket Engine is no longer part of the production system."
